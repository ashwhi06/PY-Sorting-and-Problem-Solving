# Write your solution for algorithm 2 below
#2. Write an algorithm that takes in an unsorted numerical list as an argument and creates a new sorted list (use the sorted() function).

lst = [5, 10, 4, 7, 6, 2] #unsorted list
sorted_lst = sorted(lst) # create sorted list
print(sorted_lst) #prints sorted list